const { test } = require('@playwright/test');

// NOTE: UI selectors required for the ask flow are not yet available.
// This placeholder keeps the suite structure aligned with the test plan.
test.describe.skip('Ask Service E2E', () => {
    test('placeholder - awaiting UI selectors', async () => {});
});
